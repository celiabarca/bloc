<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
{#        {% block stylesheets %}{% endblock %}#}
    {% block stylesheets %}
        <link href="{{ asset('css/mycss.css') }}" type="text/css" rel="stylesheet" />
{#        <link href="{{ asset('css/css/bootstrap.min.css') }}" type="text/css" rel="stylesheet" />
        <link href="{{ asset('js/js/bootstrap.min.js') }}" />
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>#}
    {% endblock %}
    </head>
    <body>
        {% block body %}
{#            {{ dump(app.user.username) }}#}
{#            {{ "hola mpn" }}#}
            <nav class="menu">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="#">WebSiteName</a>
                    </div>
                    <ul class="nav navbar-nav">                       
                        <li class="active"><a href="{{path("register")}}">Home</a></li>
                        {% if app.user %}
                            {% if app.user.role == 'ROLE_ADMIN' %}
{#                                <li><a href="{{path("adminPost")}}">Administrar Posts</a></li>#}
                                <li><a href="{{path("adminUsers")}}">Administrar Users</a></li>
                                <li><a href="{{path("adminPosts")}}">Administrar Posts</a></li>
                            {% endif %}
                                <li><a href="{{path("logout")}}">Desloguearte</a></li>
                                <li><a href="{{path("btncreate")}}">Crear Post</a></li>
                                <li><a href="{{path("blog")}}">Blog</a></li>
                        {% else %}
                            <li><a href="{{path("register")}}">Crear una cuenta</a></li>
                            <li><a class = "logoin btn" href="{{path("login")}}">Login</a></li>   
                        {% endif %}
                    </ul>
                </div>
            </nav>
        
        {% endblock %}
{#        {% block javascripts %}{% endblock %}#}
    </body>
</html>
